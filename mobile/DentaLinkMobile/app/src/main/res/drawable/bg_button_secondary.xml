<?xml version="1.0" encoding="utf-8"?>
<!--
    Secondary CTA button — solid mint fill matching the secondary brand color.
    Uses the same white text as PrimaryButton so both CTAs read consistently.
    The mint-green fill visually differentiates it from the teal gradient primary.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed state — deeper mint -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/secondary_dark" />
            <corners android:radius="@dimen/button_radius" />
        </shape>
    </item>

    <!-- Disabled -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/text_muted" />
            <corners android:radius="@dimen/button_radius" />
        </shape>
    </item>

    <!-- Default — secondary mint solid -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/secondary" />
            <corners android:radius="@dimen/button_radius" />
        </shape>
    </item>

</selector>
